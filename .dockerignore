.git
.gitignore
__pycache__
*.pyc
*.egg-info
dist/
build/
.venv
venv
.pytest_cache
.coverage
htmlcov/
.github
docs/
*.md
!README.md
# NOT excluded: Dokploy writes the saved environment variables to `.env` in
# the build context and expects the Dockerfile to bring it into the image.
# Excluding it here is one of the three reasons the OAuth configuration
# silently never reached the container (2026-09-07). The build context is
# private to Docker and this image is not pushed to a registry.
# .env
# .env.*
Dockerfile*
docker-compose*
.dockerignore
.DS_Store
*.log
tests/
